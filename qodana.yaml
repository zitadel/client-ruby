version: "1.0"

profile:
  name: qodana.recommended

exclude:
  - name: All
    paths:
      - lib/zitadel/client/api
      - lib/zitadel/client/models

ruby:
  version: '3.0'

bootstrap: |
  sed -i '' '/^BUNDLED WITH$/,+1d' Gemfile.lock
  bundle install

linter: jetbrains/qodana-ruby:2025.1-eap
